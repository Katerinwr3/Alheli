---
// Componente de auspiciantes
import LogoPuntoNet from '../assets/logo-puntonet-corp-sin-1.png';
import LogoUniandes from '../assets/logo blancp uniandes.png';
import LogoFonles from '../assets/logo-fonles-studio.png';
---

<section class="sponsors-section">
	<div class="sponsors-container">
		<!-- Título principal -->
		<div class="main-title">
			AUSPICIANTES:
		</div>
		
		<!-- Logos de auspiciantes -->
		<div class="sponsors-logos">
			<!-- Logo PuntoNet -->
			<div class="sponsor-logo">
				<img src={LogoPuntoNet.src} alt="PuntoNet Corp" class="logo-image" />
			</div>
			
			<!-- Logo Universidad Uniandes -->
			<div class="sponsor-logo">
				<img src={LogoUniandes.src} alt="Universidad Uniandes" class="logo-image logo-center" />
			</div>
			
			<!-- Logo Fonles Studio -->
			<div class="sponsor-logo">
				<img src={LogoFonles.src} alt="Fonles Studio" class="logo-image" />
			</div>
		</div>
		
		<!-- Texto informativo -->
		<div class="info-text">
			<p>Fonles Expo es un evento informativo. No se envían confirmaciones por este medio. El acceso a Alhelí se habilitará después del lanzamiento.</p>
			<p>Agenda, ponente y características del producto pueden variar sin previo aviso. El tratamiento de datos se realiza únicamente para la gestión del evento conforme a nuestra política de privacidad.</p>
		</div>
	</div>
</section>

<style>
	.sponsors-section {
		width: 100%;
		max-width: 100vw;
		background: #000000;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 1rem 2rem 3rem 2rem;
		font-family: sans-serif;
		box-sizing: border-box;
		overflow-x: hidden;
		position: relative;
	}

	.sponsors-container {
		max-width: 1200px;
		width: 100%;
		text-align: center;
	}

	.main-title {
		font-family: 'Montserrat', sans-serif;
		font-size: clamp(2rem, 4vw, 3rem);
		font-weight: bold;
		color: white;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		margin-bottom: 3rem;
		line-height: 1.1;
	}

	.sponsors-logos {
		display: flex;
		justify-content: center;
		align-items: center;
		gap: 3rem;
		margin-bottom: 3rem;
		flex-wrap: wrap;
	}

	.sponsor-logo {
		flex: 1;
		min-width: 150px;
		display: flex;
		justify-content: center;
		align-items: center;
		padding: 1rem;
	}

	.logo-image {
		max-width: 100%;
		height: auto;
		max-height: 80px;
		object-fit: contain;
	}

	.logo-center {
		max-height: 200px;
	}

	.info-text {
		text-align: justify;
		width: 100%;
		margin: 0 auto;
	}

	.info-text p {
		font-family: Arial, sans-serif;
		font-size: clamp(0.8rem, 1.6vw, 1.1rem);
		font-weight: normal;
		color: white;
		line-height: 1.1;
		margin: 0 0 1rem 0;
		letter-spacing: 0.02em;
		opacity: 0.8;
	}

	.info-text p:last-child {
		margin-bottom: 0;
	}

	/* Responsive */
	@media (max-width: 768px) {
		.sponsors-section {
			padding: 3rem 1.5rem 2rem 1.5rem;
		}

		.main-title {
			font-size: clamp(1.8rem, 3.5vw, 2.5rem);
			margin-bottom: 2.5rem;
		}

		.sponsors-logos {
			flex-direction: column;
			gap: 2rem;
			margin-bottom: 2.5rem;
		}

		.sponsor-logo {
			min-width: auto;
			width: 100%;
		}

		.info-text {
			text-align: center;
		}
	}

	@media (max-width: 480px) {
		.sponsors-section {
			padding: 2rem 1rem 1.5rem 1rem;
		}

		.main-title {
			font-size: clamp(1.5rem, 3vw, 2rem);
			margin-bottom: 2rem;
		}

		.sponsors-logos {
			gap: 1.5rem;
			margin-bottom: 2rem;
		}

		.sponsor-logo {
			padding: 0.8rem;
		}

		.logo-image {
			max-height: 65px;
		}

		.logo-center {
			max-height: 170px;
		}
	}

	@media (max-width: 360px) {
		.sponsors-section {
			padding: 1.5rem 0.8rem 1rem 0.8rem;
		}

		.main-title {
			font-size: clamp(1.3rem, 2.5vw, 1.8rem);
			margin-bottom: 1.8rem;
		}

		.sponsors-logos {
			gap: 1.2rem;
			margin-bottom: 1.8rem;
		}

		.sponsor-logo {
			padding: 0.6rem;
		}

		.logo-image {
			max-height: 55px;
		}

		.logo-center {
			max-height: 140px;
		}
	}

	/* Orientación landscape en móviles */
	@media (max-width: 768px) and (orientation: landscape) {
		.sponsors-section {
			padding: 2rem 1rem 1.5rem 1rem;
		}

		.sponsors-logos {
			flex-direction: row;
			gap: 1.5rem;
		}

		.sponsor-logo {
			min-width: 120px;
			padding: 0.5rem;
		}

		.logo-image {
			max-height: 50px;
		}

		.logo-center {
			max-height: 130px;
		}

		.main-title {
			font-size: clamp(1.2rem, 2.5vw, 1.8rem);
			margin-bottom: 1.5rem;
		}
	}

	/* Pantallas muy grandes */
	@media (min-width: 1400px) {
		.sponsors-section {
			padding: 5rem 3rem 4rem 3rem;
		}

		.main-title {
			font-size: 3.5rem;
			margin-bottom: 4rem;
		}

		.sponsors-logos {
			gap: 4rem;
			margin-bottom: 4rem;
		}

		.sponsor-logo {
			padding: 1.5rem;
		}

		.logo-image {
			max-height: 100px;
		}

		.logo-center {
			max-height: 220px;
		}

		.info-text p {
			font-size: 1.4rem;
		}
	}
</style>

